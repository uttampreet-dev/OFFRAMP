import Stub from "@/components/Stub";
export default function Page() {
  return <Stub title="The Bridge" subtitle="on-chain → INR" k="bridge" note="Correlates an on-chain cash-out with the matching INR bank credit on amount × FX rate × time window — a candidate linkage, stated as such." />;
}
