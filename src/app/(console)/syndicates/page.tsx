import Stub from "@/components/Stub";
export default function Page() {
  return <Stub title="Syndicates" subtitle="shared off-ramp clustering" k="syndicates" note="Groups complaints that cash out through the same downstream wallet into one suspected network with combined exposure." />;
}
