# Free keys. BTC and TRON work without them; ETH needs the Etherscan key.
ETHERSCAN_API_KEY=
TRONGRID_API_KEY=

# Session-signing secret. The build falls back to a fixed development value when unset;
# set a long random string before any deployment beyond a local demonstration.
AUTH_SECRET=
